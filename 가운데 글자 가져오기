class Solution {
    fun solution(s: String): String {
        if(s.length%2 == 0)
        return s.substring((s.length/2)-1 until (s.length/2)+1)
        else
        return s.substring((s.length/2) until (s.length/2)+1)
    }
}
