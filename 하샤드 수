class Solution {
    fun solution(x: Int): Boolean {
        var input = x
        var n:Int = 0
        while(input!=0){
            n += input%10
            input /= 10
        }
        if(x%n==0)
        return true
        else
        return false
    }
}
